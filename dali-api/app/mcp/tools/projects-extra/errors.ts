// Re-export of the canonical MCP errors + requireForAction from the leaf
// module. Kept as a local barrel so the tool files in this directory can import
// from "./errors" unchanged.
export * from "../../errors";
